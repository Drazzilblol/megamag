<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.instinctools.megamag">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ticket_details.TicketDetailsActivity" />
        <activity android:name=".presentation.announcement.AnnouncementActivity" />
        <activity android:name=".presentation.tickets.TicketsActivity" />
        <activity android:name=".presentation.info.activities.HowToBookActivity" />
        <activity android:name=".presentation.info.activities.HowToPayActivity" />
        <activity android:name=".presentation.info.activities.RulesActivity" />
        <activity android:name=".presentation.info.activities.ContactUsActivity" />
        <activity android:name=".presentation.theater_details.TheaterDetailsActivity" />
        <activity android:name=".presentation.profile.ProfileActivity"></activity>
    </application>

</manifest>